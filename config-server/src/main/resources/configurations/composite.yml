eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8080

management:
  tracing:
    sampling:
      probability: 1.0

application:
  config:
    school-url: http://localhost:8081/api/v1
    students-url: http://localhost:8082/api/v1